#include "Encode.h"

int encode(char* base, char* encode, char* out, uint8_t bitCountToEncode) {
    if (base == NULL || encode == NULL || out == NULL){
        return -1;
    }

    //if ()
    return 0;
}
